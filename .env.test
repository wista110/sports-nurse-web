# Test Environment Variables
NODE_ENV=test

# Test Database URL - will be overridden by test setup
DATABASE_URL="postgresql://test_user:test_password@localhost:5432/test_sports_nurse_matching"

# NextAuth Configuration for Testing
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=test-secret-key-for-testing-only

# Mock External Services
PUSHER_APP_ID=test-app-id
PUSHER_KEY=test-key
PUSHER_SECRET=test-secret
PUSHER_CLUSTER=test-cluster

# Email Service (Mock)
EMAIL_FROM=test@example.com
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=test
SMTP_PASSWORD=test

# File Storage (Mock)
BLOB_READ_WRITE_TOKEN=test-blob-token

# Disable external service calls in tests
DISABLE_EXTERNAL_SERVICES=true